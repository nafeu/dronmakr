#!/usr/bin/env bash
echo "dronmakr-backend sidecar placeholder — run scripts/build_sidecar.sh before release builds." >&2
ROOT="$(cd "$(dirname "$0")/../../.." && pwd)"
exec python3 "$ROOT/backend/backend_server.py" "$@"
